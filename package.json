{
  "name": "cubeforge-examples",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter platformer-example dev",
    "build:all": "pnpm -r build",
    "publish:all": "node scripts/publish-all.mjs",
    "publish:game": "node scripts/publish-game.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0"
  }
}
